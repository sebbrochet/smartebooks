{
  "$comment": "The island vocabulary an author may use. Hand-maintained for now; apps/library/src/islandContract.test.ts fails if it drifts from the real registries. Consumed by scripts/lint-content.mjs, which cannot import the engine's TypeScript directly. See SPEC006 F1.2.",
  "builtIn": ["quiz", "checkpoint", "video", "flashcard", "audio", "matching-pairs"],
  "packs": {
    "chess": ["chess-board", "chess-puzzle", "chess-analysis"]
  },
  "aliases": {
    "matchingpairs": "matching-pairs",
    "chessboard": "chess-board",
    "chesspuzzle": "chess-puzzle",
    "chessanalysis": "chess-analysis"
  },
  "attributes": {
    "checkpoint": { "label": { "type": "string" } },
    "video": { "src": { "type": "string", "required": true }, "title": { "type": "string" } },
    "audio": { "src": { "type": "string", "required": true }, "title": { "type": "string" } },
    "chess-board": {
      "theme": { "type": "enum", "values": ["brown", "blue", "green", "grey"] },
      "pieces": { "type": "enum", "values": ["cburnett", "unicode"] },
      "analysis": { "type": "boolean" }
    },
    "chess-puzzle": {
      "theme": { "type": "enum", "values": ["brown", "blue", "green", "grey"] },
      "pieces": { "type": "enum", "values": ["cburnett", "unicode"] },
      "fen": { "type": "string", "required": true }
    },
    "chess-analysis": { "fen": { "type": "string", "required": true } }
  }
}
